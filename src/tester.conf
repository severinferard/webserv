server {
    root ./src/www/test;
    listen 0.0.0.0:8889;
    server_name  www.example.*;
    index index.html index.htm;
    autoindex on;
    
    location / {
        allowed_methods GET;
    }

    location /put_test {
        allowed_methods PUT;
        client_body_temp_path ./src/www/data;
        client_max_body_size 200000000;
    }

    location $ .bla {
        allowed_methods POST;
        cgi_pass cgi_tester;
    }
    
   location /directory {
        autoindex off;
        root ./src/www/test/YoupiBanane;
        index youpi.bad_extension;
        client_max_body_size 200000000;
   }

   location /post_body {
        client_body_temp_path ./src/www/data;
        allowed_methods POST;
        client_max_body_size 100;
   }

      location /data {
        root ./src/www/data;
        allowed_methods GET;
        autoindex on;
   }
}
